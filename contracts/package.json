{
  "name": "@shadow-economy/contracts",
  "version": "0.1.0",
  "description": "EVVM smart contracts for Shadow Economy",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "clean": "hardhat clean && rm -rf artifacts cache"
  },
  "keywords": [
    "solidity",
    "ethereum",
    "evvm",
    "smart-contracts"
  ],
  "author": "Shadow Economy Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@pythnetwork/pyth-sdk-solidity": "^3.0.0"
  }
}

